---
title: Donor Wall
breadcrumbs: Donate | Donor Wall
---

import { graphql } from "gatsby"
import DonorList from 'src/components/DonorList'

Here are some of our recent donors. Thanks for your support!

<DonorList items={props.data.Donors.edges} />

export const pageQuery = graphql`
  query {
    Donors: allDonor(filter: {provider: {ne: "none"}}, sort: {fields: createdAt, order: DESC}) {
      edges {
        node {
          amount
          currency
          publicName
        }
      }
    }
  }
`