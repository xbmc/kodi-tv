---
title: Top Matrix Add-On Authors
breadcrumbs: Add-ons | Matrix | Top Authors
---

import { graphql } from "gatsby"
import ListTopAuthors from "src/components/ListTopAuthors"

<ListTopAuthors authors={props.data.allAuthor.edges} />

export const pageQuery = graphql`
  query {
    allAuthor: allMatrixAuthor(limit: 28, sort: {fields: [totaladdons, name], order: [DESC, ASC]}) {
      edges {
        node {
          name
          slug
          icon
          totaladdons
        }
      }
    }
  }
`