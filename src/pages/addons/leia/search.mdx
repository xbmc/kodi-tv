import { graphql } from "gatsby"
import SearchAddons from 'src/components/SearchAddons'
import MetadataHeader from "src/components/SiteMetadata"

<MetadataHeader title="Search Add-ons | Matrix | Addons" />

# Search Add-Ons

<SearchAddons addons={props.data.allAddon.nodes} categories={props.data.allCategory.edges} />


export const pageQuery = graphql`
  query {
    allAddon: allLeiaAddon(sort: {fields: name, order: ASC}) {
      nodes {
        addonid
        name
        icon
        slug
        downloads
        lastupdate
        description
        categories {
          name
        }
        authors {
          name
        }
      }
    }
    allCategory: allMatrixCategory(sort: {fields: name, order: ASC}) {
      edges {
        node {
          name
        }
      }
    }
  }
`