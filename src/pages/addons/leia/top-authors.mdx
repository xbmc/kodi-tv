---
title: Top Leia Add-On Authors
breadcrumbs: Add-ons | Leia | Top Authors
---

import { graphql } from "gatsby"
import ListTopAuthors from "src/components/ListTopAuthors"

<ListTopAuthors authors={props.data.allAuthor.edges} />

export const pageQuery = graphql`
  query {
    allAuthor: allLeiaAuthor(limit: 28, sort: {fields: [totaladdons, name], order: [DESC, ASC]}) {
      edges {
        node {
          name
          slug
          icon
          totaladdons
        }
      }
    }
  }
`