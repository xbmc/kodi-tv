# Awesome Add-ons Page

## Featured Add-ons

<AddonsIconList addons={props.data.allAddon.edges} />

## New Additions

<AddonsIconList addons={props.data.allAddonClone.edges} />

## Recently Updated

<AddonsIconList addons={props.data.allAddonCloneTwo.edges} />

## Categories

<CategoryIconList categories={props.data.allCategory.edges} />




import { graphql } from "gatsby"
import CategoryIconList from 'src/components/CategoryIconList'
import AddonsIconList from 'src/components/AddonsIconList'

export const pageQuery = graphql`
    query {
        allCategory(sort: {fields: name, order: ASC}) {
            edges {
                node {
                    name
                    slug
                    icon
                }
            }
        }
        allAddon(limit: 5, filter: {featured: {eq: "true"}}) {
            edges {
                node {
                    authors {
                        name
                        slug
                    }
                    description
                    disclaimer
                    forum
                    icons {
                        localpath
                    }
                    lastupdate(formatString: "MMMM DD, YYYY")
                    license
                    longname
                    name
                    news
                    platforms {
                        platform
                        path
                    }
                    size
                    source
                    summary
                    version
                    website
                }
            }
        }
        allAddonClone(limit: 5, filter: {agetype: {eq: "new"}}, sort: {fields: firstseen, order: DESC}) {
            edges {
                node {
                    authors {
                        name
                        slug
                    }
                    description
                    disclaimer
                    forum
                    icons {
                        localpath
                    }
                    lastupdate(formatString: "MMMM DD, YYYY")
                    license
                    longname
                    name
                    news
                    platforms {
                        platform
                        path
                    }
                    size
                    source
                    summary
                    version
                    website
                }
            }
        }
        allAddonCloneTwo(limit: 5, filter: {agetype: {eq: "existing"}}, sort: {fields: lastupdate, order: DESC}) {
            edges {
                node {
                    authors {
                        name
                        slug
                    }
                    description
                    disclaimer
                    forum
                    icons {
                        localpath
                    }
                    lastupdate(formatString: "MMMM DD, YYYY")
                    license
                    longname
                    name
                    news
                    platforms {
                        platform
                        path
                    }
                    size
                    source
                    summary
                    version
                    website
                }
            }
        }
    }
`