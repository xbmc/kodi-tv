import { graphql } from "gatsby"
import ListTopAuthors from "src/components/ListTopAuthors"
import MetadataHeader from "src/components/SiteMetadata"

<MetadataHeader title="Top Add-On Authors" />

# Top Add-On Authors

<ListTopAuthors authors={props.data.allAuthor.edges} />

export const pageQuery = graphql`
  query {
    allAuthor(limit: 28, sort: {fields: [totaladdons, name], order: [DESC, ASC]}) {
      edges {
        node {
          name
          slug
          icon
          totaladdons
        }
      }
    }
  }
`