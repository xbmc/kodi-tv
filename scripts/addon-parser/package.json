{
  "name": "kodi-addon-parser",
  "version": "1.0.0",
  "description": "An app to get the XML from a Kodi repo and convert it into JSON for Gatsby to consume",
  "main": "app.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build script setup\"",
    "export": "node app.mjs --kv=omega --getstats"
  },
  "author": "pkscout <pkscout@kodi.tv>",
  "license": "GPL-3-ONLY",
  "dependencies": {
    "js-yaml": "^4.1.1",
    "minimist": "^1.2.8",
    "node-fetch": "^3.3.2",
    "slugify": "^1.6.6",
    "xml-parser": "^1.2.1"
  },
  "devDependencies": {
    "@types/js-yaml": "4.0.9"
  }
}